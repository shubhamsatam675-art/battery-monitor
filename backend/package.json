{
  "name": "battery-monitor-backend",
  "version": "1.0.0",
  "description": "Simulates a Li-ion battery (charge/discharge) and exposes a REST API for its live status.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
